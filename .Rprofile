library(readr)

if (!file.exists("entradas")) {
  dir.create("entradas")
  file.create("entradas/predicoes.json")
  write_csv(datasets::iris, "entradas/iris.csv")
}

if (!file.exists("saidas")) {
  dir.create("saidas")
}
